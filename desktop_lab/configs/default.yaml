db:
  host: localhost
  port: 5432
  user: root
  password: qwerty
  db_name: lab_protocol
  ssl_mode: disable

server:
  host: localhost
  port: 8080

logger:
  level: debug
  development: true
  disable_caller: true
  disable_stacktrace: true
  encoding: json
  output_paths: stderr
  error_output_paths: stderr